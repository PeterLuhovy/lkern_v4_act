# ================================================================
# Contact Service (MDM) - Environment Variables Template
# ================================================================
# File: services/lkms101-contacts/.env.template
# Version: v1.0.0
# Created: 2025-11-08
# Description:
#   Environment configuration template for Contact Service (MDM).
#   Copy this file to .env and customize values.
# ================================================================

# Service Information
SERVICE_NAME=Contact Service (MDM)
SERVICE_VERSION=1.0.0
LKERN_ENVIRONMENT=development

# Server Configuration
REST_HOST=0.0.0.0
REST_PORT=4101
GRPC_HOST=0.0.0.0
GRPC_PORT=5101

SERVER_RELOAD=true
LOG_LEVEL=info
DEBUG=true

# Database Configuration
DB_HOST=lkms101-contacts-db
DB_PORT=5432
DB_NAME=lkern_contacts
DB_USER=lkern_admin
DB_PASSWORD=lkern_dev_password_2024

# Kafka Configuration
KAFKA_BOOTSTRAP_SERVERS=lkms504-kafka:9092
KAFKA_TOPIC_PREFIX=contacts

# CORS Configuration
CORS_ORIGINS=http://localhost:4201,http://127.0.0.1:4201
CORS_ALLOW_CREDENTIALS=true
CORS_MAX_AGE=3600

# ================================================================
# MinIO Configuration (optional - only if hasFileUpload=true)
# ================================================================
# MINIO_ENDPOINT=lkms505-minio:9000
# MINIO_ACCESS_KEY=minioadmin
# MINIO_SECRET_KEY=minioadmin
# MINIO_BUCKET_NAME=contacts
# MINIO_SECURE=false
# MAX_FILE_SIZE=10485760
